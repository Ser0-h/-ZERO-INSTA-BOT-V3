{
	"botName": "InstaBOT",
	"prefix": "-",
	"language": "en",
	"adminBot": [
		"52943709733"
	],
	"nickNameOnStart": "InstaBOT",
	"env": {
		"token": "chat.api.toke.neokex.ica.token.can.change.a9y.2ime.ok",
		"url": "https://nkx-ica.neokex.xyz",
		"notes": "Optional. `token` and `url` are read from the environment when left blank (INSTABOT_TOKEN / INSTABOT_URL or TOKEN / URL). Use them for a custom music server or any external API your commands call."
	},
	"server": {
		"url": "https://nkx-ica.neokex.xyz",
		"token": "chat.api.toke.neokex.ica.token.can.change.a9y.2ime.ok",
		"botId": "",
		"timeout": 60000,
		"notes": "REQUIRED for the deployed bot. Set `url` to your deployed ig-chat-api-server URL (https://<your-server-host>, NOT http://127.0.0.1:8787) and `token` to the server's IG_TOKEN. Leave `botId` empty so the bot identifies itself by its own Instagram account id (read from account.txt); the server keys each session by that id, so a bot always reconnects to its own session. Set it only if you filed the session under a different name. Empty url/token means read from the environment: IG_API_SERVER / IG_API_TOKEN / IG_BOT_ID."
	},
	"account": {
		"proxy": null,
		"userAgent": null,
		"notes": "Instagram login is cookie-only. Put your Instagram cookies in account.txt: a Netscape cookie file (Cookie-Editor export), a `sessionid=...; ds_user_id=...; csrftoken=...` string, or a JSON array of cookie objects. `sessionid` and `ds_user_id` are required. `proxy` is an optional HTTP(S) proxy URL and `userAgent` overrides the default Instagram Android user agent."
	},
	"music": {
		"enable": true,
		"apiUrl": "",
		"apiToken": "",
		"notes": "Song search for the sing command. Leave apiUrl/apiToken blank to search Instagram's own music catalogue. Point them at your music server to search there instead; {query} is replaced with the song text."
	},
	"antiInbox": false,
	"noPrefix": false,
	"welcome": {
		"enable": true,
		"message": "Welcome %1 to %2! 👋",
		"threadIDs": [],
		"notes": "Greet members added to a group thread. %1 = the new member, %2 = the thread name. Leave threadIDs empty for every thread, or list the thread IDs to limit it."
	},
	"leave": {
		"enable": true,
		"message": "%1 left %2. 👋",
		"threadIDs": [],
		"notes": "Announce members who leave or are removed from a group thread. %1 = the member, %2 = the thread name. Leave threadIDs empty for every thread."
	},
	"adminOnly": {
		"enable": false,
		"ignoreCommands": []
	},
	"whiteList": {
		"enable": false,
		"userIDs": [],
		"threadIDs": []
	},
	"hideNotiMessage": {
		"commandNotFound": false,
		"adminOnly": false,
		"userBanned": false,
		"needRoleToUseCommand": false
	},
	"cooldown": {
		"default": 1
	},
	"selfListen": true,
	"listenEvents": true,
	"autoMarkRead": false,
	"autoMarkDelivery": true,
	"autoReconnect": true,
	"restartListenInterval": 3600000,
	"logEvents": {
		"disableAll": false,
		"message": true,
		"message_reply": true,
		"message_reaction": true,
		"message_unsend": true,
		"read_receipt": false,
		"typ": false
	},
	"onlineStatus": {
		"enable": true,
		"file": "data/online-status.jsonl",
		"interval": 60000,
		"notes": "Appends one JSON line per interval recording that the bot is online, together with uptime and basic stats."
	},
	"database": {
		"dir": "data",
		"autoRefreshThreadInfo": true,
		"notes": "Simple JSON storage for users and threads. Files are created automatically."
	}
}

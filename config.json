{
	"botName": "InstaBOT",
	"prefix": "-",
	"language": "en",
	"adminBot": [],
	"nickNameOnStart": "InstaBOT",
	"env": {
		"token": "",
		"url": "",
		"notes": "Optional secrets/endpoints for custom commands. Read from the environment when left blank (INSTABOT_TOKEN / INSTABOT_URL). Use them for a custom music server or any external API your commands call."
	},
	"server": {
		"url": "",
		"token": "",
		"timeout": 60000,
		"notes": "Your deployed ig-chat-api-server. Set `url` to YOUR server (https://<your-server-host>, NOT http://127.0.0.1:8787) and `token` to YOUR server's IG_TOKEN. Prefer the environment variables IG_API_SERVER / IG_API_TOKEN in your host's dashboard; an environment value OVERRIDES whatever is written here, so a forked config can never point your bot at someone else's server. There is no bot id to set: the server identifies each session by the bot's own Instagram account id and returns it when the bot pushes its cookies, so the bot adopts it automatically."
	},
	"account": {
		"proxy": null,
		"userAgent": null,
		"notes": "Instagram login is cookie-only. Put your Instagram cookies in account.txt: a Netscape cookie file (Cookie-Editor export), a `sessionid=...; ds_user_id=...; csrftoken=...` string, or a JSON array of cookie objects. `sessionid` and `ds_user_id` are required. `proxy` is an optional HTTP(S) proxy URL and `userAgent` overrides the default Instagram Android user agent."
	},
	"music": {
		"enable": true,
		"apiUrl": "",
		"apiToken": "",
		"notes": "Song search for the sing command. Leave apiUrl/apiToken blank to search Instagram's own music catalogue. Point them at your music server to search there instead; {query} is replaced with the song text."
	},
	"antiInbox": false,
	"noPrefix": false,
	"welcome": {
		"enable": true,
		"message": "Welcome %1 to %2! 👋",
		"threadIDs": [],
		"notes": "Greet members added to a group thread. %1 = the new member, %2 = the thread name. Leave threadIDs empty for every thread, or list the thread IDs to limit it."
	},
	"leave": {
		"enable": true,
		"message": "%1 left %2. 👋",
		"threadIDs": [],
		"notes": "Announce members who leave or are removed from a group thread. %1 = the member, %2 = the thread name. Leave threadIDs empty for every thread."
	},
	"adminOnly": {
		"enable": false,
		"ignoreCommands": []
	},
	"whiteList": {
		"enable": false,
		"userIDs": [],
		"threadIDs": []
	},
	"hideNotiMessage": {
		"commandNotFound": false,
		"adminOnly": false,
		"userBanned": false,
		"needRoleToUseCommand": false
	},
	"cooldown": {
		"default": 1
	},
	"selfListen": true,
	"listenEvents": true,
	"autoMarkRead": false,
	"autoMarkDelivery": true,
	"autoReconnect": true,
	"restartListenInterval": 3600000,
	"logEvents": {
		"disableAll": false,
		"message": true,
		"message_reply": true,
		"message_reaction": true,
		"message_unsend": true,
		"read_receipt": false,
		"typ": false
	},
	"onlineStatus": {
		"enable": true,
		"file": "data/online-status.jsonl",
		"interval": 60000,
		"notes": "Appends one JSON line per interval recording that the bot is online, together with uptime and basic stats."
	},
	"database": {
		"dir": "data",
		"autoRefreshThreadInfo": true,
		"notes": "Simple JSON storage for users and threads. Files are created automatically."
	}
}

Custom commands placed here are loaded automatically.

Note: you can also drop command files directly into `commands/` — that is the
default location. This folder is kept for backwards compatibility.
